'''
https://leetcode.com/problems/find-the-length-of-the-longest-common-prefix/

for each number on array1
    convert numbers into string using str(num)
    go over the string, and find all the prefixes and store in set.

for each numbe in array2,
    convert numbers into string using str(num)
    find all the substrings, and check if its in previsouly created set
'''

def longest_common_prefix(arr1: list[int], arr2: list[int]):
    pass

